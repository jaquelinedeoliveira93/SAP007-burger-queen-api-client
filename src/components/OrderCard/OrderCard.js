import Button from '../Button/Button';

function OrderCard(id, clientName, table, status, processedAt, createdAt, updatedAt, Products, onClick) {
  return (
    <div>OrderCard</div>
  )
}

export default OrderCard